let value: Bool = {
    "Hello"
    42 + 12
    False
} // => False

// Expression blocks can be used instead of parentheses to change the precedence of operations.

let celsius = { fahrenheit - 32 } * 5 / 9
